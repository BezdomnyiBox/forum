# Используем базовый образ MySQL
FROM mysql:8.0

# Устанавливаем переменные окружения для MySQL
ENV MYSQL_ROOT_PASSWORD=rootpassword
ENV MYSQL_DATABASE=forum
ENV MYSQL_USER=user
ENV MYSQL_PASSWORD=password

# Экспонируем порт для MySQL
EXPOSE 3306
